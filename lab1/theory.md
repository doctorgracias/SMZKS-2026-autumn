## Лабораторная работа №1. Исследование уязвимостей веб-приложений и программных интерфейсов, настройка механизмов аутентификации

**Цель работы:** практическое освоение методов выявления и эксплуатации уязвимостей прикладного уровня (SQLi, XSS, BOLA), алгоритмов компрометации JWT-токенов, а также проектирование механизмов защиты (CSP, Secure Cookies) в соответствии со стандартами OWASP.

Инструкция по запуску:
1.	Установить Python 3.10+.
2.	Выполнить команду: `pip install -r requirements.txt`.
3.	Запустить стенд: `python app.py`. Сервер будет доступен по адресу http://127.0.0.1:5000.
4.	Для выполнения всех шагов с вашего рабочего компьютера (терминала/консоли) используется утилита `curl` для отправки HTTP-запросов и однострочники python для анализа данных.
---
Порядок выполнения лабораторной работы (Общий для всех, целевые параметры и используемые векторы зависят от индивидуального варианта (см. таблицу вариантов)).

Задание 1. Обход аутентификации через SQL-инъекцию и ее устранение
1. Выполните эксплуатацию уязвимости SQL-инъекции через эндпоинт http://127.0.0.1:5000/login с помощью консольной утилиты curl для обхода аутентификации и получения сессионного токена администратора (токен понадобится в задании 4).
2. Устраните уязвимости SQL-инъекции путем переключения запроса на параметризованный вид в работающем экземпляре приложения через консоль Python.
3. Перезапустите сервер, повторите атаку пункта 1 и убедитесь, что сервер возвращает ошибку Invalid credentials (код 401).

Задание 2. Эксплуатация межсайтового скриптинга (XSS)
1. Выполните отправку полезной нагрузки отраженного межсайтового скриптинга (Reflected XSS) через GET-запрос с помощью `curl` на эндпоинт http://127.0.0.1:5000/search?q= для демонстрации выполнения произвольного кода в контексте браузера.
2. Выполните внедрение защитного HTTP-заголовка `Content-Security-Policy` (CSP) в конфигурацию ответов сервера для блокировки выполнения несанкционированных сценариев.

Задание 3. Нарушение разграничения доступа на уровне объектов (BOLA/IDOR)
1. Выполните несанкционированное извлечение конфиденциальных данных администратора (Флаг системы) по прямому идентификатору через эндпоинт http://127.0.0.1:5000/api/profile/1 с помощью `curl`.
2. Выполните добавление проверки авторизации (валидации совпадения идентификатора сессии пользователя с запрашиваемым ID) в программную логику API.

Задание 4. Подделка и атака на JWT-токен
1.	Выполните подбор слабого секретного ключа JWT с помощью однострочника на Python в терминале и сгенерируйте поддельный токен с повышенными привилегиями (`role: admin`) для доступа к закрытой панели `/admin`.
2.	Выполните усиление параметров генерации токена в коде (переход на криптостойкий случайно генерируемый секрет фиксированной длины не менее 256 бит).

Задание 5. Проектирование механизмов защиты (Без написания кода)
1. Выполните настройку защищенных атрибутов для HTTP-заголовка Set-Cookie при выдаче сессионного токена в коде приложения для нейтрализации атак кражи сессий через XSS и перехвата трафика (MitM).
2. Выполните проектирование и внедрение HTTP-заголовка `Content-Security-Policy` с использованием индивидуальной директивы для блокировки выполнения внедренного тега `<script>`.
---
**Теоретическая база**

Протокол HTTP является stateless (не сохраняющим состояние) протоколом: каждый запрос клиента к серверу обрабатывается изолированно. Для идентификации пользователя между запросами используются файлы Cookies (куки) — небольшие фрагменты данных, которые сервер передает клиенту через заголовок Set-Cookie, а клиент автоматически возвращает в последующих запросах через заголовок Cookie.

Атрибуты безопасности Cookies:

* _HttpOnly:_ запрещает доступ к cookies со стороны клиентских скриптов (JavaScript через document.cookie), что является базовой защитой от кражи сессий при XSS-атаках.

* _Secure:_ гарантирует передачу cookies исключительно по зашифрованному протоколу HTTPS.

* _SameSite:_ защищает от межсайтовой подделки запросов (CSRF), контролируя отправку cookies при кросс-доменных запросах.

Рассматриваемые уязвимости:

1. _Инъекции кода баз данных (SQL Injection / SQLi)._
SQL-инъекция возникает в случаях, когда входные данные пользователя не проходят процедуру фильтрации, санитизации или параметризации, а напрямую конкатенируются (склеиваются) со строкой SQL-запроса, выполняемого интерпретатором базы данных. Злоумышленник может внедрить управляющие конструкции SQL-языка (например, логические операторы OR), изменив логику выполнения запроса.
Безопасный код рассматривает пользовательский ввод как данные (литералы), обрабатывая их отдельно от команд управления базой данных. Уязвимый код использует конкатенацию (склеивание строк), из-за чего интерпретатор СУБД не может отличить легитимные учетные данные от управляющих символов SQL (кавычек ', операторов OR, комментариев --). Внедрение логического условия OR 1=1 принудительно делает предикат WHERE истинным для любой строки в таблице, обходя проверку подлинности.

2. _Межсайтовый скриптинг (Cross-Site Scripting / XSS)._
Reflected (отраженный) XSS возникает, когда приложение принимает пользовательский ввод из параметров запроса (например, из строки поиска) и сразу возвращает его в теле HTTP-ответа без экранирования специальных символов (<, >, "). В результате браузер жертвы интерпретирует входящий текст как исполняемый код на языке JavaScript, выполняя его в контексте доверенной сессии пользователя. 
Уязвимость XSS возникает из-за отсутствия контекстно-зависимого экранирования (Context-Aware Output Encoding). Когда сервер возвращает пользовательский ввод в HTML-документ, специальные символы (<, >, &, ") должны быть заменены на безопасные HTML-сущности (&lt;, &gt;). Если этого не происходит, браузер жертвы переключается из режима интерпретации текста в режим интерпретации разметки, выполняя переданный сценарий (например, тег \<script>) в том же сессионном контексте. Политика CSP решает эту проблему принципиально иначе: она запрещает исполнение встроенных (inline) скриптов на уровне политик безопасности браузера, игнорируя наличие тегов в тексте.

3. _Нарушение авторизации на уровне объектов (BOLA / IDOR)._
BOLA (Broken Object Level Authorization) — уязвимость контроля доступа, возникающая при обращении к ресурсам по прямому идентификатору (например, /api/profile/1). Если бэкенд-приложение не выполняет проверку прав текущего сеанса пользователя на просмотр запрашиваемого идентификатора, злоумышленник может получить доступ к конфиденциальным данным других пользователей путем перебора ID.

JSON Web Token (JWT) — это открытый стандарт (RFC 7519) для безопасной передачи утверждений между сторонами. Токен состоит из трех частей, разделенных точками:

* _Header (Заголовок)_: указывает тип токена и алгоритм подписи (например, HS256).

* _Payload (Полезная нагрузка)_: содержит утверждения (claims) — user_id, username, role, время истечения срока (exp).

* _Signature (Подпись)_: формируется путем хеширования заголовка и полезной нагрузки с использованием секретного ключа (JWT_SECRET).
Если секретный ключ является слабым (например, коротким словом), его можно восстановить методом перебора по словарю (Brute-force), после чего злоумышленник может самостоятельно сформировать произвольный токен с измененными привилегиями (например, изменив role на admin).
Токен JWT базируется на концепции криптографической целостности, а не конфиденциальности (полезная нагрузка Payload кодируется в Base64, но не шифруется — её может прочитать любой). Безопасность токена держится исключительно на цифровой подписи (Signature), которая вычисляется по алгоритму $HMAC(Header \cdot Payload, Secret)$. Если секретный ключ является коротким словом из словаря, стойкость алгоритма сводится к нулю: злоумышленник может перебрать все варианты ключа за секунды, вычислить HMAC для измененного Payload (повысив права до admin) и выдать поддельный токен за легитимный.

Политика защиты контента (Content Security Policy / CSP) – это механизм безопасности в виде HTTP-заголовка, позволяющий владельцу веб-ресурса ограничить домены, с которых браузер имеет право загружать и выполнять скрипты, изображения и другие ресурсы, нейтрализуя атаки класса XSS.

---
**Индивидуальные варианты**

Номер варианта определяется по формуле: (Номер студента в списке журнала % 15) + 1.

| Вариант |Условие SQLi (Payload type) | Вектор XSS (Обязательный тег) | BOLA: ID жертвы  |  JWT: Целевая роль (role) | Защита: Директива CSP  |
|:--------|:--------------------------:|------------------------------:|---|---|---|
| 1       |     Boolean-based (OR)     |                     \<script> | ID = 1 (admin)  | admin  | default-src  |
| 2       |              Tautology ('=')              | \<img> с обработчиком onerror | ID = 3  | superuser  | script-src с использованием nonce  |
| 3       |           Union-based (если применимо) или OR                 |  \<svg> с обработчиком onload | ID = 4  | root  | script-src с использованием hash  |
| 4       |          Error-based (вызов ошибки БД)                  |              \<body> с onload |  ID = 5 | administrator  | strict-dynamic  |
| 5       |                       Boolean-based (OR)     |                     \<iframe> | ID = 6  | system  | Запрет unsafe-inline  |
| 6       |                   Комментарии SQL (-- или #)         |    \<a href="javascript:..."> | ID = 7  | admin  |  default-src 'self' |
| 7       |                         Tautology ('=')   |                     \<script> | ID = 8  | superuser  | script-src  |
| 8       |                          Комментарии SQL (/*)  |                     \<object> | ID = 9  | root  |  object-src 'none' |
| 9       |                       Boolean-based (OR)     |  \<input> с autofocus onfocus |  ID = 10 |  administrator |  base-uri 'none' |
| 10      |                        Error-based    |              \<img> с onerror |  ID = 11 | system  | script-src с nonce  |
| 11      |                         Tautology ('=')   |         \<details> с ontoggle |  ID = 12 | admin  | Запрет unsafe-eval  |
| 12      |                        Boolean-based (OR)    |            \<audio> с onerror | ID = 13  |  superuser | default-src  |
| 13      |                         Комментарии SQL (--)   |               \<svg> с onload |  ID = 14 | root  | script-src с hash  |
| 14      |                        Tautology ('=')    |                     \<script> | ID = 15  | administrator  | strict-dynamic  |
| 15      |                       Error-based     |              \<body> с onload | ID = 16  |  system | Запрет unsafe-inline  |


---

Отчет по лабораторной работе выполняется каждым студентом индивидуально в бумажном виде и сдается преподавателю на практическом занятии.
Номер индивидуального варианта задания определяется по формуле, указанной в методических указаниях, и отражается на титульном листе. Все задания (с 1 по 5) выполняются строго по своему варианту.
Оформление отчета должно соответствовать общим требованиям к текстовым учебным документам (шрифт Times New Roman, 14 pt, межстрочный интервал — 1.5, выравнивание по ширине).

Отчет должен содержать следующие структурные разделы:

1. Титульный лист
2. Цель работы
3. Основная часть (отчет по модулям): пошаговое описание выполнения заданий с разделением на атакующие и защитные действия для каждого из пяти модулей.
4. Заключение: краткие выводы о проделанной работе, приобретенных навыках и эффективности исследованных защитных механизмов.

Для каждого из модулей (заданий) в отчете необходимо привести:

_Задание 1 (SQL Injection):_

1. Формулировка индивидуального условия (тип payload из таблицы вариантов). 
2. Команда `curl`, использованная для проведения атаки и скриншот/текстовый вывод ответа сервера с подтверждением обхода аутентификации. 
3. Фрагмент исправленного кода на Python с использованием параметризованных запросов. 
4. Текстовый вывод консоли при повторном запуске атаки (демонстрация получения ошибки 401 Unauthorized).

_Задание 2 (XSS & CSP):_

1. Индивидуальный вектор полезной нагрузки (обязательный HTML-тег из таблицы вариантов). 
2. Команда `curl`, демонстрирующая отражение уязвимого ввода в теле HTTP-ответа. 
3. Фрагмент кода на Python, реализующий добавление заголовка Content-Security-Policy с индивидуальной директивой варианта. 
4. Скриншот или описание проверки работы CSP (с фиксацией блокировки скрипта в консоли).

_Задание 3 (BOLA / IDOR):_

1. Значение идентификатора жертвы (ID), назначенного по индивидуальному варианту. 
2. Команда `curl` и скриншот JSON-ответа сервера, содержащего конфиденциальные данные (флаг) профиля. 
3. Фрагмент защищенного кода на Python с проверкой соответствия идентификатора сессии и запрашиваемого ресурса.

_Задание 4 (JWT Forgery):_

1. Исходный слабый токен и результат его перебора (найденный секретный ключ). 
2. Фрагмент консольного скрипта на Python, использованного для подписи поддельного токена с целевой ролью из индивидуального варианта. 
3. Команда `curl` с подставленной cookies `auth_token`, демонстрирующая успешный доступ к закрытой панели администратора /admin (с выводом флага системы). 
4. Фрагмент кода на Python, демонстрирующий перевод генерации токена на криптостойкий секрет (256 бит).

_Задание 5 (Проектирование защитных механизмов):_

1. Развернутый ответ на пункт 1: корректно сформированный вызов метода set_cookie с обязательным указанием трех флагов (HttpOnly, Secure, SameSite) и академическим обоснованием их назначения. 
2. Развернутый ответ на пункт 2: спецификация HTTP-заголовка Content-Security-Policy, соответствующая индивидуальной директиве варианта, с пояснением принципа ее работы против внедренных сценариев.